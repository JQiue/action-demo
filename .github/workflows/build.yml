name: build
on:
  push:
    branches:
    - main
jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v3
    - name: Run build script
      run: |
        ls -a
        ./build.sh
      shell: bash
